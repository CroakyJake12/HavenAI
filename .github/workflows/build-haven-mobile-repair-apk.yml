name: Build Haven Mobile Repair APK (legacy manual)
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy self-committing workflow retired; use the consolidated APK workflow."
