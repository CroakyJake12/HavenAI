name: Legacy Haven Android API repair (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "Retired; use Haven Mobile Repair Android instead."
