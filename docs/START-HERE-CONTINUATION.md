# Start Here — Haven Continuation

1. Work only in `CroakyJake12/HavenAI` on `haven-continuation`.
2. Do not merge to `main`.
3. Read:
   - `docs/HAVEN-FULL-PROJECT-HANDOVER.md`
   - `docs/HAVEN-MASTER-AUDIT-CHECKPOINTS.md`
   - `docs/HAVEN-CONTINUATION-VALIDATION.md`
4. Run the full restore/build/test matrix before claiming any checkpoint verified.
5. Complete large dependency-linked tranches, not isolated scaffolding.
6. Update the checkpoint ledger and add a pass report after each major pass.
7. Use current primary documentation for external APIs/protocols.
8. Keep Ollama first-class and cloud fallback explicit.
9. Never replay model/tool actions after output, side effects, cancellation or crash.
10. Draft PR #1 is an older branch and is not the complete continuation line.
