<UserControl xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="Haven.Desktop.Views.AutomationsView">
  <Grid RowDefinitions="Auto,Auto,Auto,*" Margin="32,28">

    <Grid ColumnDefinitions="*,Auto,Auto">
      <StackPanel>
        <TextBlock Text="Scheduled Actions" Classes="heading" />
        <TextBlock Text="Scheduled local actions with persistent history, leases, and duplicate-run protection." Classes="muted" Margin="0,4,0,0" />
      </StackPanel>
      <Button x:Name="RegisterWorkerButton" Grid.Column="1" Content="Register background worker" Margin="0,0,8,0" />
      <Button x:Name="RefreshButton" Grid.Column="2" Content="Refresh" />
    </Grid>

    <Border Grid.Row="1" Classes="card" Margin="0,18,0,14">
      <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="1.2*,1.8*,Auto,Auto,2*,Auto" ColumnSpacing="8" RowSpacing="10">
        <TextBlock Grid.ColumnSpan="6" Text="CREATE SCHEDULED ACTION" Classes="eyebrow" />
        <TextBox x:Name="NewNameBox" Grid.Row="1" PlaceholderText="Name" />
        <TextBox x:Name="NewInstructionBox" Grid.Row="1" Grid.Column="1" PlaceholderText="Instruction" />
        <ComboBox x:Name="ModeSelector" Grid.Row="1" Grid.Column="2" MinWidth="100" />
        <ComboBox x:Name="ScheduleKindSelector" Grid.Row="1" Grid.Column="3" MinWidth="130" />
        <TextBox x:Name="ScheduleJsonBox" Grid.Row="1" Grid.Column="4" PlaceholderText="Schedule JSON" FontFamily="Cascadia Code, Consolas" />
        <Button x:Name="CreateButton" Grid.Row="1" Grid.Column="5" Content="Create" Classes="primary" />
      </Grid>
    </Border>

    <Border Grid.Row="2" Classes="card" Margin="0,0,0,14">
      <TextBlock x:Name="StatusText" Classes="muted" />
    </Border>

    <ScrollViewer Grid.Row="3">
      <ItemsControl x:Name="AutomationsList">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Classes="card" Margin="0,0,0,10">
              <Grid ColumnDefinitions="170,90,*,150,Auto,Auto,Auto" ColumnSpacing="10">
                <StackPanel>
                  <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding StateLabel}" Foreground="{DynamicResource HavenAccentBrush}" FontSize="10" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding Mode}" Foreground="{DynamicResource HavenAccentSecondaryBrush}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding Instruction}" Classes="muted" VerticalAlignment="Center" />
                <TextBlock Grid.Column="3" Text="{Binding NextRun}" Classes="muted" VerticalAlignment="Center" HorizontalAlignment="Right" />
                <Button Grid.Column="4" Content="Run now" />
                <Button Grid.Column="5" Content="{Binding ToggleLabel}" />
                <Button Grid.Column="6" Content="Delete" />
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
