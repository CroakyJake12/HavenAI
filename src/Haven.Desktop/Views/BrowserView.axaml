<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Haven.Desktop.Views.BrowserView">
  <Grid RowDefinitions="Auto,Auto,Auto,Auto,*">
    <Border x:Name="HorizontalTabsBorder"
            Background="{DynamicResource HavenElevatedBrush}"
            BorderBrush="{DynamicResource HavenLineBrush}"
            BorderThickness="0,0,0,1"
            Padding="10,7">
      <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="6">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
          <ItemsControl x:Name="HorizontalTabsList">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Background="{DynamicResource HavenPanelBrush}"
                        BorderBrush="{DynamicResource HavenLineBrush}"
                        BorderThickness="1"
                        CornerRadius="9"
                        Margin="0,0,5,0"
                        Padding="3">
                  <Grid ColumnDefinitions="3,*,Auto" MinWidth="155" MaxWidth="240">
                    <Border Background="{DynamicResource HavenAccentBrush}"
                            CornerRadius="3"
                            IsVisible="{Binding IsSelected}" />
                    <Button Grid.Column="1"
                            Classes="chrome"
                            HorizontalContentAlignment="Left">
                      <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="Private" Foreground="{DynamicResource HavenAccentBrush}" FontSize="9" IsVisible="{Binding IsPrivate}" />
                        <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" MaxLines="1" />
                      </StackPanel>
                    </Button>
                    <Button Grid.Column="2"
                            Content="&#x00D7;"
                            Classes="chrome"
                            HorizontalAlignment="Right"
                            ToolTip.Tip="Close tab" />
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
        <Button x:Name="NewTabButton" Grid.Column="1" Content="+" Classes="icon" ToolTip.Tip="New tab" />
        <Button x:Name="NewPrivateTabButton" Grid.Column="2" Content="Private" Classes="ghost" ToolTip.Tip="New private tab" />
      </Grid>
    </Border>

    <Grid Grid.Row="1" ColumnDefinitions="Auto,Auto,Auto,Auto,*,Auto,Auto,Auto" ColumnSpacing="6" Margin="10,8,10,6">
      <Button x:Name="BackButton" Content="&#x2190;" Classes="icon" ToolTip.Tip="Back" />
      <Button x:Name="ForwardButton" Grid.Column="1" Content="&#x2192;" Classes="icon" ToolTip.Tip="Forward" />
      <Button x:Name="HomeButton" Grid.Column="2" Content="&#x2302;" Classes="icon" ToolTip.Tip="Home" />
      <Button x:Name="ReloadButton" Grid.Column="3" Content="&#x21BB;" Classes="icon" ToolTip.Tip="Reload">
        <Button.ContextMenu>
          <ContextMenu>
            <MenuItem x:Name="ReloadMenuItem" Header="Reload" />
            <MenuItem x:Name="HardReloadMenuItem" Header="Hard reload and clear site cache" />
            <MenuItem x:Name="StopMenuItem" Header="Stop" />
          </ContextMenu>
        </Button.ContextMenu>
      </Button>
      <TextBox x:Name="AddressBox" Grid.Column="4" PlaceholderText="Search or enter address" KeyDown="OnAddressKeyDown" />
      <Button x:Name="NavigateButton" Grid.Column="5" Content="Go" Classes="accent" MinWidth="52" />
      <Button x:Name="AddBookmarkButton" Grid.Column="6" Content="&#x2606;" Classes="icon" ToolTip.Tip="Save this page to bookmarks" />
      <Button x:Name="MenuButton" Grid.Column="7" Content="&#x22EF;" Classes="icon" ToolTip.Tip="Browser menu">
        <Button.Flyout>
          <MenuFlyout>
            <MenuItem x:Name="BookmarksMenuItem" Header="Bookmarks" />
            <MenuItem x:Name="HistoryMenuItem" Header="History" />
            <MenuItem x:Name="LoginsMenuItem" Header="Passwords and logins" />
            <MenuItem x:Name="ExtensionsMenuItem" Header="Extensions" />
            <Separator />
            <MenuItem x:Name="PrintMenuItem" Header="Print" />
            <MenuItem x:Name="InspectMenuItem" Header="Inspect" />
            <Separator />
            <MenuItem x:Name="AssistantMenuItem" Header="Haven side assistant" />
            <MenuItem x:Name="SettingsMenuItem" Header="Browser settings" />
          </MenuFlyout>
        </Button.Flyout>
      </Button>
    </Grid>

    <Border x:Name="BookmarkBar" Grid.Row="2"
            Background="{DynamicResource HavenPanelBrush}"
            BorderBrush="{DynamicResource HavenLineBrush}"
            BorderThickness="0,1,0,1"
            Padding="10,5">
      <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
        <TextBlock Text="Bookmarks" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="10" />
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
          <Grid>
            <TextBlock x:Name="NoBookmarksText" Text="Save the current page with the star button"
                       Classes="muted2" FontSize="10" VerticalAlignment="Center" />
            <ItemsControl x:Name="BookmarksBarList" IsVisible="False">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="4" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Button Classes="ghost" ToolTip.Tip="{Binding Address}" MaxWidth="180">
                    <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </Grid>
        </ScrollViewer>
        <Button x:Name="ManageBookmarksButton" Grid.Column="2" Content="Manage" Classes="ghost" ToolTip.Tip="Open bookmark manager" />
      </Grid>
    </Border>

    <Grid Grid.Row="3" ColumnDefinitions="*,Auto,Auto" Margin="14,4,14,7">
      <TextBlock x:Name="PrivacyLabel" Classes="muted" FontSize="10" />
      <TextBlock x:Name="GroupLabel" Grid.Column="1" Foreground="{DynamicResource HavenAccentSecondaryBrush}" FontSize="10" Margin="0,0,14,0" />
      <TextBlock x:Name="StatusLabel" Grid.Column="2" Classes="muted" FontSize="10" MaxWidth="440" TextTrimming="CharacterEllipsis" />
    </Grid>

    <Grid Grid.Row="4" ColumnDefinitions="Auto,*,Auto">
      <Border x:Name="VerticalTabsBorder" Width="232"
              Background="{DynamicResource HavenElevatedBrush}"
              BorderBrush="{DynamicResource HavenLineBrush}"
              BorderThickness="0,1,1,0">
        <Grid RowDefinitions="Auto,*,Auto">
          <Grid ColumnDefinitions="*,Auto" Margin="12,10,9,7">
            <StackPanel>
              <TextBlock Text="Open tabs" FontWeight="SemiBold" />
              <TextBlock Text="Vertical browser tabs" Classes="muted2" FontSize="9" />
            </StackPanel>
            <Button x:Name="VerticalNewTabButton" Grid.Column="1" Content="+" Classes="icon" ToolTip.Tip="New tab" />
          </Grid>
          <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="VerticalTabsList" Margin="7,0">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <Border Background="{DynamicResource HavenPanelBrush}"
                          BorderBrush="{DynamicResource HavenLineBrush}"
                          BorderThickness="1"
                          CornerRadius="8"
                          Margin="0,0,0,6">
                    <Grid ColumnDefinitions="3,*,Auto" MinHeight="47">
                      <Border Background="{DynamicResource HavenAccentBrush}"
                              CornerRadius="3"
                              IsVisible="{Binding IsSelected}" />
                      <Button Grid.Column="1"
                              Classes="chrome"
                              HorizontalContentAlignment="Stretch"
                              Padding="9,6">
                        <Grid RowDefinitions="Auto,Auto">
                          <TextBlock Text="{Binding DisplayTitle}" TextTrimming="CharacterEllipsis" MaxLines="1" />
                          <TextBlock Grid.Row="1" Text="{Binding Group}" Classes="muted2" FontSize="9" TextTrimming="CharacterEllipsis" />
                        </Grid>
                      </Button>
                      <Button Grid.Column="2"
                              Content="&#x00D7;"
                              Classes="chrome"
                              ToolTip.Tip="Close tab" />
                    </Grid>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
          <Button x:Name="VerticalNewPrivateTabButton" Grid.Row="2" Content="New private tab" Classes="ghost" Margin="8" />
        </Grid>
      </Border>

      <Border Grid.Column="1"
              Background="{DynamicResource HavenPanelBrush}"
              BorderBrush="{DynamicResource HavenLineStrongBrush}"
              BorderThickness="1,1,0,0"
              ClipToBounds="True">
        <Grid>
          <NativeWebView x:Name="NativeBrowser" />
          <Border x:Name="LoadingOverlay" Background="{DynamicResource HavenPanelBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
              <ProgressBar Width="190" Height="4" IsIndeterminate="True" />
              <TextBlock Text="Loading page…" Classes="muted" HorizontalAlignment="Center" />
            </StackPanel>
          </Border>
        </Grid>
      </Border>

      <Border x:Name="SidePanel" Grid.Column="2"
              Width="360"
              Background="{DynamicResource HavenElevatedBrush}"
              BorderBrush="{DynamicResource HavenLineBrush}"
              BorderThickness="1,1,0,0">
        <Grid>
          <ScrollViewer x:Name="BookmarksPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="10">
              <Grid ColumnDefinitions="*,Auto">
                <StackPanel>
                  <TextBlock Text="Bookmarks" Classes="sectionHeading" />
                  <TextBlock x:Name="BookmarkSummary" Classes="muted" FontSize="10" />
                </StackPanel>
                <Button x:Name="CloseBookmarksButton" Grid.Column="1" Content="Close" Classes="ghost" />
              </Grid>
              <TextBlock Text="Choose a group, then save the page currently shown in the address bar." TextWrapping="Wrap" Classes="muted" FontSize="11" />
              <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                <TextBox x:Name="BookmarkGroupBox" PlaceholderText="Bookmark group" />
                <Button x:Name="SaveBookmarkButton" Grid.Column="1" Content="Save current page" Classes="accent" />
              </Grid>
              <Border x:Name="NoBookmarksPanel" Classes="soft" Padding="12">
                <StackPanel Spacing="4">
                  <TextBlock Text="No bookmarks yet" FontWeight="SemiBold" />
                  <TextBlock Text="Saved pages appear here and in the bookmark bar." Classes="muted" FontSize="10" TextWrapping="Wrap" />
                </StackPanel>
              </Border>
              <ItemsControl x:Name="BookmarksList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Classes="card" Margin="0,3" Padding="9">
                      <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                        <Button Classes="chrome"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left">
                          <StackPanel Spacing="2">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Address}" Classes="muted" FontSize="10" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding Group}" Foreground="{DynamicResource HavenAccentSecondaryBrush}" FontSize="9" />
                          </StackPanel>
                        </Button>
                        <Button Grid.Column="1" Content="Delete" Classes="danger" ToolTip.Tip="Delete bookmark" />
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer x:Name="HistoryPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="9">
              <Grid ColumnDefinitions="*,Auto">
                <TextBlock Text="History" Classes="sectionHeading" />
                <Button x:Name="ClearHistoryButton" Grid.Column="1" Content="Clear" Classes="danger" />
              </Grid>
              <ItemsControl x:Name="HistoryList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Button Classes="sidebar" HorizontalAlignment="Stretch">
                      <StackPanel>
                        <TextBlock Text="{Binding Title}" />
                        <TextBlock Text="{Binding Address}" Classes="muted" FontSize="10" TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding VisitedAt}" Classes="muted2" FontSize="9" />
                      </StackPanel>
                    </Button>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer x:Name="AssistantPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="9">
              <TextBlock Text="Haven side assistant" Classes="sectionHeading" />
              <TextBlock Text="Uses visible page text and your selected local model." Classes="muted" FontSize="11" />
              <Button x:Name="SummariseButton" Content="Summarise page" />
              <TextBox x:Name="AssistantInputBox" PlaceholderText="Ask about this page" AcceptsReturn="True" MinHeight="70" />
              <Button x:Name="AskAssistantButton" Content="Ask" Classes="accent" />
              <SelectableTextBlock x:Name="AssistantOutputText" Classes="soft" LineHeight="21" />
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer x:Name="SettingsPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="9">
              <TextBlock Text="Browser settings" Classes="sectionHeading" />
              <TextBlock Text="Tabs, history, credentials, and extensions stay in Haven's isolated profile." Classes="muted" FontSize="11" />
              <TextBox x:Name="HomePageBox" PlaceholderText="Home page" />
              <TextBox x:Name="SearchTemplateBox" PlaceholderText="Search URL containing {query}" />
              <CheckBox x:Name="SaveHistoryCheckBox" Content="Save history" />
              <CheckBox x:Name="OfferToSaveLoginsCheckBox" Content="Offer to save logins" />
              <CheckBox x:Name="RestoreTabsCheckBox" Content="Restore tabs" />
              <CheckBox x:Name="EnableExtensionsCheckBox" Content="Enable approved extensions" />
              <CheckBox x:Name="VerticalTabsCheckBox" Content="Vertical browser tabs" />
              <TextBox x:Name="NewGroupBox" PlaceholderText="Current tab group" />
              <Button x:Name="CreateGroupButton" Content="Put current tab in group" />
              <Button x:Name="SaveSettingsButton" Content="Save browser settings" Classes="accent" />
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer x:Name="ExtensionsPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="9">
              <TextBlock Text="Extensions" Classes="sectionHeading" />
              <TextBlock Text="Imports are disabled initially. Chrome conversion accepts only bounded content scripts and rejects privileged permissions." Classes="muted" FontSize="11" />
              <Grid ColumnDefinitions="*,*" ColumnSpacing="6">
                <Button x:Name="ImportExtensionButton" Content="Import Haven extension" />
                <Button x:Name="ConvertChromeButton" Grid.Column="1" Content="Convert Chrome extension" />
              </Grid>
              <ItemsControl x:Name="ExtensionsList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Classes="card" Margin="0,4">
                      <StackPanel Spacing="5">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Description}" Classes="muted" FontSize="10" />
                        <TextBlock Text="{Binding AllowedOrigins}" Classes="muted2" FontSize="9" />
                        <Grid ColumnDefinitions="*,Auto">
                          <Button Content="Enable / disable" />
                          <Button Grid.Column="1" Content="Delete" Classes="danger" />
                        </Grid>
                      </StackPanel>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer x:Name="LoginsPanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="16" Spacing="9">
              <TextBlock Text="Passwords and logins" Classes="sectionHeading" />
              <TextBlock Text="Passwords are encrypted by Windows Credential Manager. Autofill requires the exact saved origin and never submits." Classes="muted" FontSize="11" />
              <TextBox x:Name="LoginOriginBox" PlaceholderText="https://example.com" />
              <TextBox x:Name="LoginUsernameBox" PlaceholderText="Username" />
              <TextBox x:Name="LoginPasswordBox" PasswordChar="&#x25CF;" PlaceholderText="Password" />
              <Button x:Name="SaveLoginButton" Content="Save securely" Classes="accent" />
              <ItemsControl x:Name="LoginsList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Border Classes="card" Margin="0,4">
                      <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="5">
                        <StackPanel>
                          <TextBlock Text="{Binding Username}" />
                          <TextBlock Text="{Binding Origin}" Classes="muted" FontSize="10" />
                        </StackPanel>
                        <Button Grid.Column="1" Content="Autofill" />
                        <Button Grid.Column="2" Content="Delete" Classes="danger" />
                      </Grid>
                    </Border>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </ScrollViewer>
        </Grid>
      </Border>
    </Grid>
  </Grid>
</UserControl>
