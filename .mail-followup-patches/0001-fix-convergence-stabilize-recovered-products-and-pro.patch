From ae82bda6e500a1cad14245154bd55cb720b76e9b Mon Sep 17 00:00:00 2001
From: CroakyJake12 <jcbailey008@gmail.com>
Date: Sun, 23 Aug 2026 21:13:31 +0100
Subject: [PATCH] fix(convergence): stabilize recovered products and projects
 IDE

---
 .../Modes/BuiltInModeSeed.cs                  |  4 +--
 .../Interface/Shell/MainView.Spaces.cs        |  2 +-
 .../Interface/Shell/MainView.axaml.cs         |  4 +--
 .../Pages/Boards/BoardsPage.Attachments.cs    |  8 ++---
 .../Views/Pages/Boards/BoardsPage.cs          |  6 ++--
 .../Views/Pages/Mail/MailPage.axaml           | 12 +++----
 .../Present/PresentSlideCanvas.Structured.cs  | 24 -------------
 .../Views/Pages/Present/PresentSlideCanvas.cs | 16 +++++++--
 .../StudioProjectPage.SourceControl.cs        | 14 ++++----
 .../WorkspaceEditorPage.LanguageServer.cs     |  2 +-
 .../Calendar/OAuthCalendarTransportBase.cs    |  2 +-
 .../FileMailDraftStoreTests.cs                | 36 ++++++++++++++++++-
 .../PresentThumbnailNavigatorKeyboardTests.cs |  1 +
 13 files changed, 76 insertions(+), 55 deletions(-)

diff --git a/src/Haven.Application/Modes/BuiltInModeSeed.cs b/src/Haven.Application/Modes/BuiltInModeSeed.cs
index 45bf462..bcaabd7 100644
--- a/src/Haven.Application/Modes/BuiltInModeSeed.cs
+++ b/src/Haven.Application/Modes/BuiltInModeSeed.cs
@@ -150,8 +150,8 @@ public sealed class BuiltInModeSeed
         new ModeDefinition(
             Guid.Parse("a0000000-0000-0000-0000-000000000023"),
             "spaces", "Spaces", "Create persistent AI workspaces with their own chats, files and layout", "folder",
-            HavenMode.Chat, "["Spaces"]", "[]", "[]", "[]", "Use Haven Spaces as persistent workspace context. Keep ordinary chats unscoped unless the user explicitly opens or creates them inside a Space.",
-            ModeSource.BuiltIn, ModeInstallState.BuiltIn, "Haven", "1.0.0", "["productivity","general"]",
+            HavenMode.Chat, "[\"Spaces\"]", "[]", "[]", "[]", "Use Haven Spaces as persistent workspace context. Keep ordinary chats unscoped unless the user explicitly opens or creates them inside a Space.",
+            ModeSource.BuiltIn, ModeInstallState.BuiltIn, "Haven", "1.0.0", "[\"productivity\",\"general\"]",
             DateTimeOffset.MinValue, DateTimeOffset.MinValue),
         new ModeDefinition(
             Guid.Parse("a0000000-0000-0000-0000-000000000021"),
diff --git a/src/Haven.Desktop/Interface/Shell/MainView.Spaces.cs b/src/Haven.Desktop/Interface/Shell/MainView.Spaces.cs
index d86c928..63c501f 100644
--- a/src/Haven.Desktop/Interface/Shell/MainView.Spaces.cs
+++ b/src/Haven.Desktop/Interface/Shell/MainView.Spaces.cs
@@ -34,7 +34,7 @@ public sealed partial class MainView
 
     private async Task OpenOrStartSpaceAsync(SpaceDefinition space)
     {
-        var existing = (await _conversations.GetBySpaceAsync(space.Id, CancellationToken.None))
+        var existing = (await _conversations.GetBySpaceAsync(space.Id, 500, CancellationToken.None))
             .Where(conversation => !conversation.IsArchived)
             .OrderByDescending(conversation => conversation.UpdatedAt)
             .FirstOrDefault();
diff --git a/src/Haven.Desktop/Interface/Shell/MainView.axaml.cs b/src/Haven.Desktop/Interface/Shell/MainView.axaml.cs
index 900439d..4a7d047 100644
--- a/src/Haven.Desktop/Interface/Shell/MainView.axaml.cs
+++ b/src/Haven.Desktop/Interface/Shell/MainView.axaml.cs
@@ -121,7 +121,7 @@ public sealed partial class MainView : UserControl, INotifyPropertyChanged, IDis
     private NewChatPage? _newChatPage;
     private PlanPageViewModel? _planPage;
     private TerminalPage? _terminalPage;
-    private MailPage? _mailPage;
+    private Haven.Desktop.Views.Pages.Mail.MailPage? _mailPage;
     private PlayPage? _playPage;
     private readonly DispatcherTimer _reminderTimer;
     private int _isPollingReminders;
@@ -821,7 +821,7 @@ public sealed partial class MainView : UserControl, INotifyPropertyChanged, IDis
 
     private void OpenMail()
     {
-        _mailPage ??= new MailPage();
+        _mailPage ??= new Haven.Desktop.Views.Pages.Mail.MailPage();
         AddOrSelectTab("mail", "Mail", _mailPage, false, HavenSurface.Mail);
         ApplyShellVisualState();
     }
diff --git a/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.Attachments.cs b/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.Attachments.cs
index 9092ffa..07dfdff 100644
--- a/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.Attachments.cs
+++ b/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.Attachments.cs
@@ -28,8 +28,8 @@ public sealed partial class BoardsPage
             await SaveAsync("Added Boards attachment");
         }
         catch (OperationCanceledException) { SetStatus("Attachment import cancelled."); }
-        catch (UnauthorizedAccessException ex) { SetStatus("Attachment permission was denied."); }
-        catch (Exception ex) { SetStatus("Couldn’t add that attachment."); }
+        catch (UnauthorizedAccessException) { SetStatus("Attachment permission was denied."); }
+        catch (Exception) { SetStatus("Couldn’t add that attachment."); }
     }
 
     private async Task AddEmbedAsync()
@@ -67,7 +67,7 @@ public sealed partial class BoardsPage
                 _ => "Attachment unavailable · " + result.Message
             };
         }
-        catch (Exception ex)
+        catch (Exception)
         {
             if (!_disposed) state.Text = "Attachment unavailable.";
         }
@@ -76,7 +76,7 @@ public sealed partial class BoardsPage
     {
         if (block.Html is null) return;
         var source = new TextBox { Text = block.Html.HtmlSource, AcceptsReturn = true, MinHeight = 120 };
-        var fallback = new TextBox { Text = block.Html.FallbackText, Watermark = "Accessible fallback text" };
+        var fallback = new TextBox { Text = block.Html.FallbackText, PlaceholderText = "Accessible fallback text" };
         source.TextChanged += (_, _) => { block.Html.HtmlSource = source.Text ?? string.Empty; _document!.UpdatedAt = DateTimeOffset.UtcNow; SetStatus("Unsaved embed changes"); };
         fallback.TextChanged += (_, _) => { block.Html.FallbackText = fallback.Text ?? string.Empty; _document!.UpdatedAt = DateTimeOffset.UtcNow; SetStatus("Unsaved embed changes"); };
         host.Children.Add(source);
diff --git a/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.cs b/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.cs
index a809942..89ed011 100644
--- a/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.cs
+++ b/src/Haven.Desktop/Views/Pages/Boards/BoardsPage.cs
@@ -24,7 +24,7 @@ public sealed partial class BoardsPage : UserControl, IDisposable
     private readonly StackPanel _library = new() { Spacing = 6 };
     private readonly StackPanel _sections = new() { Spacing = 5 };
     private readonly StackPanel _editor = new() { Spacing = 10 };
-    private readonly TextBox _search = new() { Watermark = "Search notebooks..." };
+    private readonly TextBox _search = new() { PlaceholderText = "Search notebooks..." };
     private readonly TextBlock _status = new() { Text = "Opening Boards...", FontSize = 11 };
     private readonly TextBox _notebookTitle = new() { Text = "Boards", FontSize = 24, FontWeight = Avalonia.Media.FontWeight.SemiBold };
     private readonly NativeTabStrip _pageTabs = new() { Name = "Boards.PageTabs" };
@@ -171,7 +171,7 @@ public sealed partial class BoardsPage : UserControl, IDisposable
             else if (_notebooks.Count == 0)
                 ShowEmptyState();
         }
-        catch (Exception ex)
+        catch (Exception)
         {
             SetStatus("Couldn’t load Boards.");
         }
@@ -283,7 +283,7 @@ public sealed partial class BoardsPage : UserControl, IDisposable
             SetStatus($"Saved locally · {DateTimeOffset.Now:t}");
             _bus.Fire("Boards.Saved");
         }
-        catch (Exception ex)
+        catch (Exception)
         {
             SetStatus("Couldn’t save this board.");
         }
diff --git a/src/Haven.Desktop/Views/Pages/Mail/MailPage.axaml b/src/Haven.Desktop/Views/Pages/Mail/MailPage.axaml
index e4bd550..4449591 100644
--- a/src/Haven.Desktop/Views/Pages/Mail/MailPage.axaml
+++ b/src/Haven.Desktop/Views/Pages/Mail/MailPage.axaml
@@ -21,7 +21,7 @@
       </StackPanel>
 
       <Grid Grid.Column="1" ColumnDefinitions="*,Auto,Auto" MaxWidth="650" Margin="24,0">
-        <TextBox Grid.Column="0" Text="{Binding SearchText, Mode=TwoWay}" Watermark="Search mail"/>
+        <TextBox Grid.Column="0" Text="{Binding SearchText, Mode=TwoWay}" PlaceholderText="Search mail"/>
         <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}" Margin="8,0,0,0"/>
         <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshCommand}" Margin="8,0,0,0"/>
       </Grid>
@@ -223,12 +223,12 @@
             </StackPanel>
             <Button Grid.Column="1" Content="×" Click="OnCloseComposeClick"/>
           </Grid>
-          <TextBox Grid.Row="1" Text="{Binding ComposeTo, Mode=TwoWay}" TextChanged="OnComposeTextChanged" Watermark="To (separate recipients with ; )" Margin="0,10,0,0"/>
+          <TextBox Grid.Row="1" Text="{Binding ComposeTo, Mode=TwoWay}" TextChanged="OnComposeTextChanged" PlaceholderText="To (separate recipients with ; )" Margin="0,10,0,0"/>
           <Grid Grid.Row="2" ColumnDefinitions="*,*" Margin="0,8,0,0">
-            <TextBox Grid.Column="0" Text="{Binding ComposeCc, Mode=TwoWay}" TextChanged="OnComposeTextChanged" Watermark="Cc" Margin="0,0,4,0"/>
-            <TextBox Grid.Column="1" Text="{Binding ComposeBcc, Mode=TwoWay}" TextChanged="OnComposeTextChanged" Watermark="Bcc" Margin="4,0,0,0"/>
+            <TextBox Grid.Column="0" Text="{Binding ComposeCc, Mode=TwoWay}" TextChanged="OnComposeTextChanged" PlaceholderText="Cc" Margin="0,0,4,0"/>
+            <TextBox Grid.Column="1" Text="{Binding ComposeBcc, Mode=TwoWay}" TextChanged="OnComposeTextChanged" PlaceholderText="Bcc" Margin="4,0,0,0"/>
           </Grid>
-          <TextBox Grid.Row="3" Text="{Binding ComposeSubject, Mode=TwoWay}" TextChanged="OnComposeTextChanged" Watermark="Subject" Margin="0,8,0,0"/>
+          <TextBox Grid.Row="3" Text="{Binding ComposeSubject, Mode=TwoWay}" TextChanged="OnComposeTextChanged" PlaceholderText="Subject" Margin="0,8,0,0"/>
           <Border Grid.Row="4" Margin="0,8,0,0" CornerRadius="12" ClipToBounds="True" BorderThickness="1" BorderBrush="{DynamicResource HavenBorderBrush}">
             <controls:MailRichTextEditor x:Name="ComposeEditor"/>
           </Border>
@@ -277,7 +277,7 @@
         <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="420" MaxWidth="420" Padding="20" CornerRadius="18" Background="{DynamicResource HavenPanelBrush}">
           <StackPanel Spacing="12">
             <TextBlock Text="{Binding FolderEditorTitle}" FontSize="21" FontWeight="SemiBold"/>
-            <TextBox Text="{Binding FolderName, Mode=TwoWay}" Watermark="Name"/>
+            <TextBox Text="{Binding FolderName, Mode=TwoWay}" PlaceholderText="Name"/>
             <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
               <Button Content="Cancel" Command="{Binding CloseFolderEditorCommand}"/>
               <Button Content="Save" Command="{Binding SaveFolderCommand}" Padding="18,8"/>
diff --git a/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.Structured.cs b/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.Structured.cs
index 063a858..08354f9 100644
--- a/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.Structured.cs
+++ b/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.Structured.cs
@@ -7,29 +7,6 @@ internal sealed partial class PresentSlideCanvas
 {
     internal HavenRect GetElementEditorBounds(PresentElement element) => ElementRect(element, SlideRectLocal(), false);
 
-    private void DrawTransformHandles(HavenDrawingContext context, HavenRect rect, double opacity)
-    {
-        var accent = new HavenPen(new HavenTokenBrush("Accent"), 1.5);
-        var rotateCenter = new HavenPoint(rect.X + rect.Width / 2, rect.Y - 20);
-        context.Add(new HavenLineCommand(new HavenPoint(rect.X + rect.Width / 2, rect.Y), rotateCenter, accent, opacity));
-        context.Add(new HavenEllipseCommand(new HavenRect(rotateCenter.X - 5, rotateCenter.Y - 5, 10, 10), new HavenTokenBrush("SurfaceRaised"), accent, opacity));
-        context.Add(new HavenFillRoundedRectCommand(new HavenRect(rect.Right - 5, rect.Bottom - 5, 10, 10), new HavenTokenBrush("SurfaceRaised"), 2, opacity));
-        context.Add(new HavenStrokeRoundedRectCommand(new HavenRect(rect.Right - 5, rect.Bottom - 5, 10, 10), accent, 2, opacity));
-    }
-
-    private TransformHandleHit? HitTransformHandle(HavenPoint local)
-    {
-        if (_slide is null || _selectedIds.Count != 1) return null;
-        var element = _slide.Elements.FirstOrDefault(value => _selectedIds.Contains(value.Id) && !value.Locked);
-        if (element is null || element.Kind == PresentElementKind.Group || _vectorPointEditingElementId == element.Id) return null;
-        var rect = ElementRect(element, SlideRectLocal(), false);
-        var resize = new HavenRect(rect.Right - 9, rect.Bottom - 9, 18, 18);
-        if (resize.Contains(local)) return new TransformHandleHit(element.Id, PresentTransformHandleKind.Resize);
-        var rotateCenter = new HavenPoint(rect.X + rect.Width / 2, rect.Y - 20);
-        var rotate = new HavenRect(rotateCenter.X - 9, rotateCenter.Y - 9, 18, 18);
-        return rotate.Contains(local) ? new TransformHandleHit(element.Id, PresentTransformHandleKind.Rotate) : null;
-    }
-
     private static void DrawTable(HavenDrawingContext context, PresentTable table, HavenRect rect, double opacity)
     {
         context.Add(new HavenFillRoundedRectCommand(rect, new HavenTokenBrush("SurfaceRaised"), 4, opacity));
@@ -92,5 +69,4 @@ internal sealed partial class PresentSlideCanvas
         }
     }
 
-    private readonly record struct TransformHandleHit(Guid ElementId, PresentTransformHandleKind Kind);
 }
diff --git a/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.cs b/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.cs
index ad16e24..f807d65 100644
--- a/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.cs
+++ b/src/Haven.Desktop/Views/Pages/Present/PresentSlideCanvas.cs
@@ -14,6 +14,7 @@ internal sealed partial class PresentSlideCanvas : HavenElement, IHavenDrawComma
     private HashSet<Guid> _selectedIds = [];
     private Guid? _dragElementId;
     private VectorHandleHit? _dragVectorHandle;
+    private Guid? _vectorPointEditingElementId;
     private HavenPoint _pointerStart;
     private HavenPoint _pointerCurrent;
 
@@ -26,6 +27,15 @@ internal sealed partial class PresentSlideCanvas : HavenElement, IHavenDrawComma
     public event Action<Guid?>? SelectionRequested;
     public event Action<double, double>? MoveSelectionRequested;
     public event Action<Guid, Guid, PresentVectorHandleKind, double, double>? VectorHandleMoveRequested;
+    public Guid? VectorPointEditingElementId => _vectorPointEditingElementId;
+
+    public void SetVectorPointEditing(Guid? elementId)
+    {
+        _vectorPointEditingElementId = elementId;
+        _dragVectorHandle = null;
+        Invalidate();
+    }
+
 
     public void SetSlide(PresentDocument document, PresentSlide slide, IEnumerable<Guid> selectedIds)
     {
@@ -173,7 +183,7 @@ internal sealed partial class PresentSlideCanvas : HavenElement, IHavenDrawComma
             case PresentElementKind.Shape when element.VectorShape is { } vector:
                 var renderedVector = BuildPreviewVector(element, vector);
                 DrawVectorShape(context, renderedVector, rect, opacity * element.Opacity);
-                if (_selectedIds.Count == 1 && _selectedIds.Contains(element.Id))
+                if (_vectorPointEditingElementId == element.Id && _selectedIds.Count == 1 && _selectedIds.Contains(element.Id))
                     DrawVectorHandles(context, renderedVector, rect, opacity);
                 break;
             case PresentElementKind.Shape:
@@ -261,8 +271,8 @@ internal sealed partial class PresentSlideCanvas : HavenElement, IHavenDrawComma
 
     private VectorHandleHit? HitVectorHandle(HavenPoint local)
     {
-        if (_slide is null || _selectedIds.Count != 1) return null;
-        var element = _slide.Elements.FirstOrDefault(value => _selectedIds.Contains(value.Id) && !value.Locked && value.VectorShape is not null);
+        if (_slide is null || _selectedIds.Count != 1 || _vectorPointEditingElementId is null) return null;
+        var element = _slide.Elements.FirstOrDefault(value => value.Id == _vectorPointEditingElementId && _selectedIds.Contains(value.Id) && !value.Locked && value.VectorShape is not null);
         if (element?.VectorShape is not { } shape) return null;
         var rect = ElementRect(element, SlideRectLocal(), false);
         var point = InverseRotatePoint(local, rect, element.RotationDegrees);
diff --git a/src/Haven.Desktop/Views/Pages/StudioProject/StudioProjectPage.SourceControl.cs b/src/Haven.Desktop/Views/Pages/StudioProject/StudioProjectPage.SourceControl.cs
index 7c0bb82..6cd329b 100644
--- a/src/Haven.Desktop/Views/Pages/StudioProject/StudioProjectPage.SourceControl.cs
+++ b/src/Haven.Desktop/Views/Pages/StudioProject/StudioProjectPage.SourceControl.cs
@@ -31,9 +31,9 @@ public sealed partial class StudioProjectPage
         {
             SourceControl = await _intelligence.GetSourceControlAsync(RootPath, CancellationToken.None);
         }
-        catch (Exception ex)
+        catch (Exception)
         {
-            Status = "Source control refresh failed: " + ex.Message;
+            Status = "Source control could not be refreshed. Check the project repository and try again.";
         }
     }
 
@@ -41,34 +41,34 @@ public sealed partial class StudioProjectPage
     {
         if (change is null || !HasRoot) return;
         try { SourceControl = await _intelligence.StageAsync(RootPath, change.Path, CancellationToken.None); Status = $"Staged {change.Path}."; await RefreshAsync(); }
-        catch (Exception ex) { Status = "Could not stage file: " + ex.Message; }
+        catch (Exception) { Status = "That file could not be staged. Refresh source control and try again."; }
     }
 
     private async Task UnstageSourceControlAsync(ProjectSourceControlChange? change)
     {
         if (change is null || !HasRoot) return;
         try { SourceControl = await _intelligence.UnstageAsync(RootPath, change.Path, CancellationToken.None); Status = $"Unstaged {change.Path}."; await RefreshAsync(); }
-        catch (Exception ex) { Status = "Could not unstage file: " + ex.Message; }
+        catch (Exception) { Status = "That file could not be unstaged. Refresh source control and try again."; }
     }
 
     private async Task CheckoutSourceControlBranchAsync(ProjectGitBranch? branch)
     {
         if (branch is null || branch.IsCurrent || !HasRoot) return;
         try { SourceControl = await _intelligence.CheckoutBranchAsync(RootPath, branch.Name, CancellationToken.None); Status = $"Checked out {branch.Name} through the safe Git path."; await RefreshAsync(); }
-        catch (Exception ex) { Status = "Branch was not changed: " + ex.Message; }
+        catch (Exception) { Status = "The branch could not be changed safely. Refresh source control and try again."; }
     }
 
     private async Task CreateSourceControlStashAsync()
     {
         if (!HasRoot) return;
         try { SourceControl = await _intelligence.CreateStashAsync(RootPath, StashMessage, CancellationToken.None); StashMessage = string.Empty; Status = "Created a Git stash without deleting it from history."; await RefreshAsync(); }
-        catch (Exception ex) { Status = "Stash was not created: " + ex.Message; }
+        catch (Exception) { Status = "The stash could not be created. Your working files were left unchanged."; }
     }
 
     private async Task ApplySourceControlStashAsync(ProjectGitStash? stash)
     {
         if (stash is null || !HasRoot) return;
         try { SourceControl = await _intelligence.ApplyStashAsync(RootPath, stash.Reference, CancellationToken.None); Status = $"Applied {stash.Reference}. The stash was retained for recovery."; await RefreshAsync(); }
-        catch (Exception ex) { Status = "Stash was not applied: " + ex.Message; }
+        catch (Exception) { Status = "The stash could not be applied. The stash was retained for recovery."; }
     }
 }
diff --git a/src/Haven.Desktop/Views/Pages/WorkspaceEditor/WorkspaceEditorPage.LanguageServer.cs b/src/Haven.Desktop/Views/Pages/WorkspaceEditor/WorkspaceEditorPage.LanguageServer.cs
index ddc863c..4ff68c0 100644
--- a/src/Haven.Desktop/Views/Pages/WorkspaceEditor/WorkspaceEditorPage.LanguageServer.cs
+++ b/src/Haven.Desktop/Views/Pages/WorkspaceEditor/WorkspaceEditorPage.LanguageServer.cs
@@ -351,5 +351,5 @@ public sealed class CodeActionItemViewModel
 public sealed class SemanticTokenItemViewModel(CodeSemanticToken definition)
 {
     public CodeSemanticToken Definition { get; } = definition;
-    public string Display => $"{definition.Range.Start.Line + 1}:{definition.Range.Start.Character + 1} · {definition.TokenType}{(definition.Modifiers.Count == 0 ? string.Empty : " · " + string.Join(", ", definition.Modifiers))}";
+    public string Display => $"{Definition.Range.Start.Line + 1}:{Definition.Range.Start.Character + 1} · {Definition.TokenType}{(Definition.Modifiers.Count == 0 ? string.Empty : " · " + string.Join(", ", Definition.Modifiers))}";
 }
diff --git a/src/Haven.Infrastructure/Calendar/OAuthCalendarTransportBase.cs b/src/Haven.Infrastructure/Calendar/OAuthCalendarTransportBase.cs
index 3964ea7..47bd762 100644
--- a/src/Haven.Infrastructure/Calendar/OAuthCalendarTransportBase.cs
+++ b/src/Haven.Infrastructure/Calendar/OAuthCalendarTransportBase.cs
@@ -149,7 +149,7 @@ public abstract class OAuthCalendarTransportBase(
             return result;
         }
         catch (OperationCanceledException) { throw; }
-        catch (HttpRequestException ex)
+        catch (HttpRequestException)
         {
             var result = new CalendarSyncResult(false, CalendarSyncStatus.Offline, 0, 0, 0, 0, $"{Kind} Calendar is offline.");
             await repository.UpsertCalendarAccountAsync(account with { Status = result.Status, StatusMessage = result.Message, UpdatedAt = DateTimeOffset.UtcNow }, cancellationToken).ConfigureAwait(false);
diff --git a/tests/Haven.Desktop.Tests/FileMailDraftStoreTests.cs b/tests/Haven.Desktop.Tests/FileMailDraftStoreTests.cs
index ef53307..ae67e40 100644
--- a/tests/Haven.Desktop.Tests/FileMailDraftStoreTests.cs
+++ b/tests/Haven.Desktop.Tests/FileMailDraftStoreTests.cs
@@ -38,4 +38,38 @@ public sealed class FileMailDraftStoreTests : IDisposable
 
         Assert.NotNull(restored);
         Assert.Equal(localId, restored.LocalId);
-       
\ No newline at end of file
+        Assert.Equal(accountId, restored.AccountId);
+        Assert.Equal("provider-draft-42", restored.ProviderDraftId);
+        Assert.Equal(MailResponseKind.ReplyAll, restored.ResponseKind);
+        Assert.Equal("source-message-7", restored.SourceMessageId);
+        Assert.Equal("thread-9", restored.ThreadId);
+        Assert.Equal("Rich draft", restored.Subject);
+        Assert.Equal("<p>Hello <strong>world</strong></p>", restored.Body);
+        Assert.True(restored.IsHtml);
+        Assert.Single(restored.To); Assert.Single(restored.Cc); Assert.Single(restored.Bcc);
+        var attachment = Assert.Single(restored.Attachments);
+        Assert.Equal(attachmentId, attachment.LocalId);
+        Assert.Equal(new byte[] { 1, 2, 3, 4 }, attachment.Content);
+        Assert.Equal(CalendarProviderKind.Google, restored.Provider);
+        Assert.Equal(updatedAt, restored.UpdatedAt);
+        Assert.Equal(MailDraftPersistenceState.Saved, restored.PersistenceState);
+        Assert.Equal("safe marker", restored.LastSafeError);
+    }
+
+    public void Dispose()
+    {
+        try { if (Directory.Exists(_root)) Directory.Delete(_root, true); }
+        catch (IOException) { }
+        catch (UnauthorizedAccessException) { }
+    }
+
+    private sealed class TestAppPaths(string root) : IAppPaths
+    {
+        public string DataDirectory => root;
+        public string DatabasePath => Path.Combine(root, "haven.db");
+        public string BrowserProfileDirectory => Path.Combine(root, "browser");
+        public string AttachmentsDirectory => Path.Combine(root, "attachments");
+        public string LogsDirectory => Path.Combine(root, "logs");
+        public string LegacyStatePath => Path.Combine(root, "legacy.json");
+    }
+}
diff --git a/tests/Haven.Desktop.Tests/PresentThumbnailNavigatorKeyboardTests.cs b/tests/Haven.Desktop.Tests/PresentThumbnailNavigatorKeyboardTests.cs
index 7f53165..97f49ad 100644
--- a/tests/Haven.Desktop.Tests/PresentThumbnailNavigatorKeyboardTests.cs
+++ b/tests/Haven.Desktop.Tests/PresentThumbnailNavigatorKeyboardTests.cs
@@ -1,3 +1,4 @@
+using Haven.Core;
 using Haven.Desktop.Views.Pages.Present;
 using Haven.UI;
 
-- 
2.55.0.windows.2

